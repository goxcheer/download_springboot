server:
    port: 8000
    servlet:
      context-path: /
      tomcat:
          uri-encoding: UTF-8

spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/db_download?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
      username: root
      password: 604721660
   http:
     encoding:
       charset: UTF-8
       force: true
       enabled: true
